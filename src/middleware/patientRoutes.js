const express = require('express');
const router = express.Router();

// Define routes for patient-related functionality
router.get('/', (req, res) => {
  // Route logic for /patient
});

router.post('/', (req, res) => {
  // Route logic for /patient (POST)
});

// 

module.exports = router;
